select strftime('%s', endzeit.gehen) AS Endzeit, strftime('%s', anfangszeit.gehen) AS ANFANG, (strftime('%s', endzeit.gehen)-strftime('%s',anfangszeit.gehen))*1000  AS DIfferenz FROM endzeit JOIN anfangszeit ON endzeit.id_user = anfangszeit.id_user WHERE anfangszeit.id_user=4711